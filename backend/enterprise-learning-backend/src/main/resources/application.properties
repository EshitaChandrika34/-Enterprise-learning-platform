# Server Configuration
server.port=8090

# Spring Application
spring.application.name=enterprise-learning-backend

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:enterprise_learning}?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Eshita@123}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# JWT Configuration
app.jwt.secret=9a6747f6f12c45f002d664f005f32f915b3fec4bf3ad82d39a6529fa1fe45a07
app.jwt.expiration-milliseconds=86400000

# Logging
logging.level.com.enterpriselearning=INFO
logging.level.org.springframework.security=WARN

# OpenAI Career AI Configuration
